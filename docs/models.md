spawning models (DispatchSpawn() mandatory)


models are entities -> prop_script or prop_dynamic unless they are prop_static (static props) integrated into the map, no longer counted as separate entities


model keyvalues (entity.kv)

.GetOnwer()

.SetOwner()

.GetParent()

.SetParent()

.Solid()

model asset keyvalue

