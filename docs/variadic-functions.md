# Variadic Functions


Variadic functions are functions that have an unset amount of parameters and can take a variable number of arguments when they are called


varg

void function variadicFunctionName( arg1, arg2, ...) // variable number of args

The args are stored in an array 